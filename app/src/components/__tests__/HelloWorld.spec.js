import { describe, it, expect } from 'vitest'

import { mount } from '@vue/test-utils'

describe('HelloWorld', () => {
  it('renders properly', () => {
    
    expect("always true").toContain('always true')
  })
})
